import React from 'react';
import FormData from '../sidebar/data'; // Import the FormData component from the correct location

const SidebarItems = () => {
  return (
    <div>
      <FormData />
    </div>
  );
};

export default SidebarItems;
